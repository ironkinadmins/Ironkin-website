import { clearRate, failRate, getConfig, makeCookie, rateStatus, verifyPassword } from "./_teamAccess.js";
const headers=(x={})=>({"Cache-Control":"no-store",...x});
export async function onRequestPost({request,env}){const body=await request.json().catch(()=>({}));const team=["team1","team2"].includes(body.team)?body.team:null;const password=String(body.password||"");if(!team||!password||password.length>128)return Response.json({error:"Invalid team or password."},{status:400,headers:headers()});const rate=await rateStatus(request,env,team);if(!rate.allowed)return Response.json({error:"Too many attempts. Try again in 15 minutes."},{status:429,headers:headers({"Retry-After":"900"})});const c=await getConfig(env);if(!(await verifyPassword(c,team,password))){await failRate(env,rate.key);return Response.json({error:"Invalid team or password."},{status:403,headers:headers()});}await clearRate(env,rate.key);return Response.json({ok:true,team,name:c[team].name},{headers:headers({"Set-Cookie":await makeCookie(team,env,c)})});}
